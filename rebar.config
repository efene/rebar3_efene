{erl_opts, [debug_info]}.

%% The rebar3_efene_compile/ct/shell sub-plugins are vendored under src/vendor/
%% (see src/vendor/README.md). The only remaining dependency is efene itself,
%% which provides the compiler the plugin drives.
{deps, [{efene, "0.99.3"}]}.
